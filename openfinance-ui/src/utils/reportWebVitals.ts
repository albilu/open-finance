/**
 * Reports Core Web Vitals metrics to the console in development,
 * and could be extended to send to an analytics endpoint.
 * Requirement REQ-3.1: Performance monitoring.
 *
 * Note: web-vitals package is not installed. This is a no-op stub.
 * To enable real reporting, run: npm install web-vitals
 * and restore the import from 'web-vitals'.
 */
const reportWebVitals = (_onPerfEntry?: (metric: unknown) => void): void => {
  // no-op: web-vitals package not installed
};

export default reportWebVitals;
