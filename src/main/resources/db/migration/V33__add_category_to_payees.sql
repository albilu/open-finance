-- V34__add_category_to_payees.sql
-- Add default category to payees table (NOOP - column already exists in database)
-- This migration was manually applied before file creation
-- The column was added via: ALTER TABLE payees ADD COLUMN category_id BIGINT;

-- No changes needed - column already exists in database
